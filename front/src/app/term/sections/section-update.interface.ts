import {Document} from "../../core/document.interface";
//import {Section} from "../sections/section.interface";

export interface SectionUpdate extends Document {

    id: null,
    capacity: string,
    ordinal: string,
    //section: Section;
    //staff: Staff;
  }
